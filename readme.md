# README
_Generated with Copilot_
This repository exists solely for practicing Git and GitHub workflows.


## Purpose
- Try commits, branches, merges, pull requests, and CI without affecting production code.

## Usage
- Clone: `git clone <repo-url>`
- Create a branch: `git checkout -b my-branch`
- Make changes, then: `git add . && git commit -m "message"`
- Push: `git push -u origin my-branch`
- Open a pull request on GitHub to practice reviews and merges.

## Notes
- No production code; safe to experiment and delete branches.
- Delete or replace this repo when no longer needed.